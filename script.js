const money = 1000;
let income = '';
const addExpenses = '';
const deposit = 100000;
const mission = 'Accumulate a deposit';
const period = '12';

alert(money);
console.log(`${mission} ${deposit}$ for ${period} month`);